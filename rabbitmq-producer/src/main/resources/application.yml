# 配置RabbitMQ的基本信息
spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    # 消息投递确认机制
    publisher-confirm-type: correlated
    # 消息投递回退机制
    publisher-returns: true